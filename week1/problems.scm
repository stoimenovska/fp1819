;; #lang racket
;; #lang r5rs

(define (even? number) ())

(define (sides-of-triangle? a b c) ())

(define (decimal-to-binary number))

(define (factorial number) ())

(define (sum start end) ())

(define (power number exp) ())